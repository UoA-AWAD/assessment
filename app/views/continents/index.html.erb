<p> Welcome to the World Energy Analysis website.
On these pages you will find interesting data concerning global population, use of land, 
consumption of energy and emissions. Queries can be done in the search bar below to search for individual countries,
visiting the individual country pages shows more information on them and allows the user to request JSON data.
Click the link below to go to an interactive world map which allows for more intuitive interpretation of the data.</p>
<%=link_to 'World Map', home_index_path%> 
<br><br>
<%= form_tag(continents_path, method: :get) do %>
  <%= text_field_tag :term, @continents %>
  <%= submit_tag 'Search' %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Continent</th>
      <th>Country</th>
      <th>Population</th>
      <th>Population density</th>
      <th>Urban population</th>
      <th>Urban population coastal</th>
      <th>Gdp per capita</th>
      <th>Land area</th>
      <th>Cropland area</th>
      <th>Pasture area</th>
      <th>Water per capita</th>
      <th>Commercial energy consumption</th>
      <th>Traditional fuel consumption</th>
      <th>Commercial hydroelectric consumption</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @continents.each do |continent| %>
      <tr>
        <td><%= continent.Continent %></td>
        <td><%= continent.Country %></td>
        <td><%= continent.Population %></td>
        <td><%= continent.Population_density %></td>
        <td><%= continent.Urban_population %></td>
        <td><%= continent.Urban_population_coastal %></td>
        <td><%= continent.GDP_per_capita %></td>
        <td><%= continent.Land_area %></td>
        <td><%= continent.Cropland_area %></td>
        <td><%= continent.Pasture_area %></td>
        <td><%= continent.Water_per_capita %></td>
        <td><%= continent.Commercial_energy_consumption %></td>
        <td><%= continent.Traditional_fuel_consumption %></td>
        <td><%= continent.Commercial_hydroelectric_consumption %></td>
        <td><%= link_to 'Show', continent %></td>
      </tr>
    <% end %>
  </tbody>
</table>
